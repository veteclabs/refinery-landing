---
// 법적 고지 문서용 플레이스홀더.
// src/data/legal.ts의 값이 비어 있으면 노란 칩으로 [항목명]을 보여준다.
// 게시 전에 노란 칩이 하나도 남지 않았는지 확인할 것.
interface Props {
  value?: string;
  label: string;
}
const { value, label } = Astro.props;
---
{value ? value : <span class="legal-ph">[{label}]</span>}
